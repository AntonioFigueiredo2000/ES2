package src;

public class Enums {

    public enum Criteria {
        STRONG,
        WEAK
    }

    public enum StorageType{
        LOCAL,
        FILE,
        DB
    }

    public enum EncryptMethod{
        AES,
        FILE,
        DB
    }
}
