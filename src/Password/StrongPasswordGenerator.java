package src.Password;

// Concrete implementation for generating a strong password
public class StrongPasswordGenerator implements IPasswordGenerator {
    @Override
    public String generate() {
        // Logic for generating a strong password with robust criteria
        return "A!s9@Lm#2P";
    }
}